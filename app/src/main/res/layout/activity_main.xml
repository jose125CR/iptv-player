<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_app_gradient"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <!-- ============================================================
         MAIN CONTENT (tabs + content area)
         ============================================================ -->
    <LinearLayout
        android:id="@+id/mainContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:orientation="vertical">

        <!-- One chrome row: brand + a single scroller holding every chrome item - the tabs,
             Search, then Settings/Refresh at the end.
             Height is wrap_content, not a fixed toolbar_height: the tallest child (the
             tabs) decides how tall the bar is, so it tracks the content instead of holding
             a constant box. clipChildren false on the row so the tabs' 1.06x focus scale
             isn't sliced top/bottom (they're taller than the row's other children). -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="@dimen/toolbar_padding_horizontal"
            android:paddingEnd="@dimen/toolbar_padding_horizontal"
            android:paddingTop="4dp"
            android:paddingBottom="6dp">

            <!-- Left group: brand + tab scroller. weight=1 absorbs the free middle of the
                 row, so the button cluster stays pinned right even when the scroller is
                 GONE (simple mode, search open, no provider). The brand is plain
                 wrap_content inside the group - no weight - so the first tab (Live TV)
                 sits directly against the icon, and the scroller (0dp + weight=1) takes
                 exactly the remaining width: all tabs at full size on TV, a scrolling
                 window on a portrait phone. clipChildren false so the tabs' focus scale
                 isn't sliced by the group's bounds. -->
            <!-- height is wrap_content, NOT match_parent: in a wrap_content horizontal
                 LinearLayout a match_parent child's own height doesn't count toward the row's
                 height, so once the row gained a second, fixed-height child (the clock) the
                 row sized itself to that child and the group was remeasured down to it -
                 collapsing every tab to ~21dp, with the labels measured to zero height and
                 only the selection indicator left drawing. -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- App mark in place of the title text: the launcher's TV + antenna + play
                     glyph redrawn monochrome in the toolbar's icon stroke language, so the
                     brand reads crisp with no colour blob against the dark toolbar.
                     Top-aligned (layout_gravity="top") so it hugs the row's top corner like
                     a logo instead of floating centred next to the tabs, and pulled slightly
                     left via translationX (toolbar_brand_translate_x) - translation is
                     post-layout, so the scroller and tabs keep their positions; only the
                     icon moves. Decorative - not focusable, so the D-pad never lands on it. -->
                <ImageView
                    android:layout_width="@dimen/toolbar_brand_size"
                    android:layout_height="@dimen/toolbar_brand_size"
                    android:layout_gravity="top"
                    android:contentDescription="@string/app_name"
                    android:src="@drawable/ic_toolbar_brand"
                    android:tint="@color/text_primary"
                    android:translationX="@dimen/toolbar_brand_translate_x"
                    android:translationY="@dimen/toolbar_brand_translate_y" />

                <!-- Tab scroller: Home | Live | Series | Films | Discover (+ Downloads on
                     phones) with D-pad focus (pill segmented control). weight=1 makes it
                     flexible: it takes everything the brand doesn't, so on TV the tabs
                     render full-size right next to the icon and on a ~360dp portrait phone
                     it collapses to a narrow window that scrolls - every tab is still
                     reachable, and the scroller keeps the focused tab in view.

                     clipChildren/clipToPadding false on both levels because each tab is
                     height=match_parent and scales to 1.06x on focus (focus_scale_flat) -
                     without this the scale-up overflows and gets clipped, making the focus
                     highlight look cut off. The tabBar id lives on the scroller because
                     code toggles its visibility (simple mode, search open). -->
                <!-- clipChildren is deliberately TRUE here, unlike its parents. With it
                     false the scroller painted its overflow *outside* its own bounds, so
                     once the tabs stopped fitting (adding Catch Up was enough) the last of
                     them rendered straight over the Settings/Refresh cluster instead of
                     scrolling under it. Clipping is what makes the scroller behave as one.
                     The focus scale (1.06x, focus_scale_flat) still has room because
                     clipToPadding stays false and the 4dp vertical padding below gives the
                     scaled pill a band to bleed into. -->
                <HorizontalScrollView
                    android:id="@+id/tabBar"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:clipChildren="true"
                    android:clipToPadding="false"
                    android:paddingVertical="4dp"
                    android:fillViewport="true"
                    android:scrollbars="none">

        <!-- FlowRow, not LinearLayout: on a portrait phone these nine items (six tabs, Search,
             Settings, Refresh) are ~800dp of content inside a ~260dp window, so as one line
             everything past the second tab could only be reached by dragging the scroller
             through three screens. FlowRow.wrap is turned on for portrait phones only (see
             applyChromeWrap()) and breaks them onto a second line at full label width; on TV
             and in landscape wrap stays off and this lays out exactly as the LinearLayout
             did - one line, natural widths, scrolled by the parent. -->
        <com.lumora.ui.FlowRow
            android:id="@+id/tabBarRow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:paddingHorizontal="10dp"
            android:paddingBottom="6dp">

            <LinearLayout
                android:id="@+id/tabLive"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/tab_live_start_gap"
                android:layout_marginEnd="3dp"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/tab_padding_horizontal"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:nextFocusLeft="@id/tabDownloads"
                android:nextFocusRight="@id/tabSeries"
                android:stateListAnimator="@animator/focus_scale_flat"
                android:layerType="none">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/tabLiveIcon"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:src="@drawable/ic_live_tv"
                        android:tint="@color/text_secondary" />

                    <!-- Label and caret share a nested row so the caret stays beside the label
                         when applyChromeWrap() stacks the pill on a portrait phone (it flips
                         the group above to vertical - without this nesting the caret would
                         land on a line of its own under the label). -->
                    <LinearLayout
                        android:id="@+id/tabLiveLabelGroup"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:gravity="center_vertical"
                        android:orientation="horizontal">

                        <TextView
                            android:id="@+id/tabLiveLabel"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:ellipsize="end"
                            android:fontFamily="@font/inter_medium"
                            android:maxLines="1"
                            android:text="@string/tab_live_tv"
                            android:textColor="@color/text_secondary"
                            android:textSize="@dimen/tab_text" />

                        <!-- Catch Up lives in a dropdown hung off this tab rather than in a chip
                             under the tab row: the chip pushed every Live TV screen down by its
                             own height, on the one pane that has the least room to spare. Shown
                             only when some enabled provider reports tv_archive channels (see
                             updateCatchupTabVisibility) - otherwise the tab has nothing to drop
                             down and the caret would be a lie. -->
                        <ImageView
                            android:id="@+id/tabLiveChevron"
                            android:layout_width="20dp"
                            android:layout_height="20dp"
                            android:layout_marginStart="6dp"
                            android:importantForAccessibility="no"
                            android:src="@drawable/ic_chevron_down"
                            android:tint="@color/text_tertiary"
                            android:visibility="gone" />
                    </LinearLayout>
                </LinearLayout>

                <View
                    android:id="@+id/tabLiveIndicator"
                    android:layout_width="24dp"
                    android:layout_height="4dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/tab_indicator"
                    android:visibility="gone" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/tabSeries"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="3dp"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/tab_padding_horizontal"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:nextFocusLeft="@id/tabLive"
                android:nextFocusRight="@id/tabFilms"
                android:stateListAnimator="@animator/focus_scale_flat"
                android:layerType="none">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/tabSeriesIcon"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:src="@drawable/ic_series"
                        android:tint="@color/text_secondary" />

                    <TextView
                        android:id="@+id/tabSeriesLabel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:ellipsize="end"
                        android:fontFamily="@font/inter_medium"
                        android:maxLines="1"
                        android:text="@string/series_tab"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/tab_text" />
                </LinearLayout>

                <View
                    android:id="@+id/tabSeriesIndicator"
                    android:layout_width="24dp"
                    android:layout_height="4dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/tab_indicator"
                    android:visibility="gone" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/tabFilms"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="3dp"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/tab_padding_horizontal"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:nextFocusLeft="@id/tabSeries"
                android:nextFocusRight="@id/tabHome"
                android:stateListAnimator="@animator/focus_scale_flat"
                android:layerType="none">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/tabFilmsIcon"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:src="@drawable/ic_films"
                        android:tint="@color/text_secondary" />

                    <TextView
                        android:id="@+id/tabFilmsLabel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:ellipsize="end"
                        android:fontFamily="@font/inter_medium"
                        android:maxLines="1"
                        android:text="@string/films_tab"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/tab_text" />
                </LinearLayout>

                <View
                    android:id="@+id/tabFilmsIndicator"
                    android:layout_width="24dp"
                    android:layout_height="4dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/tab_indicator"
                    android:visibility="gone" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/tabHome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="3dp"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/tab_padding_horizontal"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:nextFocusLeft="@id/tabFilms"
                android:nextFocusRight="@id/tabDiscover"
                android:stateListAnimator="@animator/focus_scale_flat"
                android:layerType="none">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/tabHomeIcon"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:src="@drawable/ic_home"
                        android:tint="@color/text_secondary" />

                    <TextView
                        android:id="@+id/tabHomeLabel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:ellipsize="end"
                        android:fontFamily="@font/inter_medium"
                        android:maxLines="1"
                        android:text="@string/tab_home"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/tab_text" />
                </LinearLayout>

                <View
                    android:id="@+id/tabHomeIndicator"
                    android:layout_width="24dp"
                    android:layout_height="4dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/tab_indicator"
                    android:visibility="gone" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/tabDiscover"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="3dp"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/tab_padding_horizontal"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:nextFocusLeft="@id/tabHome"
                android:nextFocusRight="@id/btnSearch"
                android:stateListAnimator="@animator/focus_scale_flat"
                android:layerType="none">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/tabDiscoverIcon"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:src="@drawable/ic_discover"
                        android:tint="@color/text_secondary" />

                    <TextView
                        android:id="@+id/tabDiscoverLabel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:ellipsize="end"
                        android:fontFamily="@font/inter_medium"
                        android:maxLines="1"
                        android:text="@string/discover_tab"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/tab_text" />
                </LinearLayout>

                <View
                    android:id="@+id/tabDiscoverIndicator"
                    android:layout_width="24dp"
                    android:layout_height="4dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/tab_indicator"
                    android:visibility="gone" />
            </LinearLayout>

            <!-- Search: an exact copy of the tab items, id btnSearch. It isn't a tab - it
                 opens the search overlay - so it never renders the selected state or the
                 indicator (updateTabStyles never touches it and the indicator stays GONE),
                 but it must look identical to the tabs so search reads as one of them.
                 btnSearch keeps the id the old toolbar pill had: MainActivity wires the
                 click and the visibility gating by this id, and both apply unchanged to
                 this element. -->
            <LinearLayout
                android:id="@+id/btnSearch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="3dp"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/tab_padding_horizontal"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:nextFocusLeft="@id/tabDiscover"
                android:nextFocusRight="@id/btnSettings"
                android:stateListAnimator="@animator/focus_scale_flat"
                android:layerType="none">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/btnSearchIcon"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:src="@drawable/ic_search"
                        android:tint="@color/text_secondary" />

                    <TextView
                        android:id="@+id/btnSearchLabel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:ellipsize="end"
                        android:fontFamily="@font/inter_medium"
                        android:maxLines="1"
                        android:text="@string/search"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/tab_text" />
                </LinearLayout>

                <View
                    android:layout_width="24dp"
                    android:layout_height="4dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/tab_indicator"
                    android:visibility="gone" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/tabDownloads"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="3dp"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/tab_padding_horizontal"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:nextFocusLeft="@id/btnSearch"
                android:nextFocusRight="@id/btnSettings"
                android:stateListAnimator="@animator/focus_scale_flat"
                android:layerType="none"
                android:visibility="gone">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/tabDownloadsIcon"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:src="@drawable/ic_downloads"
                        android:tint="@color/text_secondary" />

                    <TextView
                        android:id="@+id/tabDownloadsLabel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:ellipsize="end"
                        android:fontFamily="@font/inter_medium"
                        android:maxLines="1"
                        android:text="@string/downloads_tab"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/tab_text" />
                </LinearLayout>

                <View
                    android:id="@+id/tabDownloadsIndicator"
                    android:layout_width="24dp"
                    android:layout_height="4dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/tab_indicator"
                    android:visibility="gone" />
            </LinearLayout>

            <!-- Settings / Refresh: the row's utility pair, last in the same scroller as the
                 tabs so the chrome reads as one bar rather than a bar plus a detached button
                 cluster. They keep their old placement (right end, Settings then Refresh)
                 and stay icon-only - a label each would push the real tabs off a phone's
                 window for two actions nobody hunts for by name. Vertical padding matches
                 the tabs (tab_padding_vertical) so they sit in the same band; horizontal
                 padding stays chrome_button_padding_h so a bare icon doesn't render as a
                 wide empty capsule. marginStart = chrome_cluster_gap on Settings keeps the
                 pair distinct from the last tab. Because they now live inside the scroller,
                 nothing may hide the scroller to hide "the tabs" - see setBrowseTabsVisible(). -->
            <LinearLayout
                android:id="@+id/btnSettings"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/chrome_cluster_gap"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:layerType="none"
                android:nextFocusLeft="@id/btnSearch"
                android:nextFocusRight="@id/btnRefresh"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/chrome_button_padding_h"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:stateListAnimator="@animator/focus_scale_flat">

                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:src="@drawable/ic_settings"
                    android:tint="@color/text_secondary" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/btnRefresh"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/toolbar_button_gap"
                android:background="@drawable/bg_tab_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:layerType="none"
                android:nextFocusLeft="@id/btnSettings"
                android:nextFocusRight="@id/btnRefresh"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/chrome_button_padding_h"
                android:paddingTop="@dimen/tab_padding_vertical"
                android:paddingBottom="@dimen/tab_padding_vertical"
                android:stateListAnimator="@animator/focus_scale_flat">

                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:src="@drawable/ic_refresh"
                    android:tint="@color/text_secondary" />
            </LinearLayout>
        </com.lumora.ui.FlowRow>
        </HorizontalScrollView>
            </LinearLayout>

            <!-- Local clock. Sits at the end of the chrome row rather than in a row of its
                 own above it: its own row cost the whole screen that row's height, on every
                 tab, to show one line of text. Outside the tab scroller (a sibling of the
                 left group, not a member of it) so it stays pinned right and can't scroll
                 away with the tabs on a narrow window - the reason it was a separate row
                 before. Decorative - never focusable, so the D-pad can't land on it. Text
                 is set from code (updateToolbarClock) in the device's 12/24h format. -->
            <!-- Aligned to the icon band, not to the row's centre: the tabs are taller than
                 their labels (each carries a selection indicator underneath), so centring the
                 clock in the row hung it visibly below the labels and the Settings/Refresh
                 icons it sits next to. Top-aligned by the tabs' own vertical padding, then
                 centred inside a 20dp box - the same band the toolbar icons occupy. -->
            <TextView
                android:id="@+id/toolbarClock"
                android:layout_width="wrap_content"
                android:layout_height="20dp"
                android:layout_gravity="top"
                android:layout_marginTop="@dimen/tab_padding_vertical"
                android:layout_marginStart="@dimen/toolbar_button_gap"
                android:fontFamily="@font/inter_medium"
                android:gravity="center_vertical"
                android:maxLines="1"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/toolbar_clock_text" />
        </LinearLayout>

        <!-- Status row: spinner (only for in-progress messages, see setStatus()) + text.
             Fills the same content slot every tab uses (0dp/weight=1) so a "Loading..."
             message - startup or switching tabs - renders big and dead-center on screen
             instead of a small line pinned under the tab bar. -->
        <LinearLayout
            android:id="@+id/statusRow"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="horizontal"
            android:padding="10dp"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/statusSpinner"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginEnd="14dp"
                android:indeterminate="true"
                android:indeterminateTint="@color/primary_light"
                android:visibility="gone" />

            <TextView
                android:id="@+id/statusText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:textColor="@color/primary_light"
                android:textSize="@dimen/text_title_s" />
        </LinearLayout>

        <!-- Empty state / QR prompt when no channels -->
        <LinearLayout
            android:id="@+id/emptyState"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/no_provider_configured"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/content_body_text" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:text="@string/press_settings_to_add_playlist"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/text_caption" />

            <!-- First run: no provider configured yet, so nothing above knows what the
                 user is here for. One entry point - an IPTV/Jellyfin provider is required
                 before there is anything to browse. nextFocus is fixed explicitly since a
                 GONE sibling (this whole block hides the moment a provider exists) must
                 never be left as an implicit focus-search target. -->
            <LinearLayout
                android:id="@+id/emptyStartupChooser"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:gravity="center"
                android:orientation="vertical">

                <LinearLayout
                    android:id="@+id/emptyChooseProvider"
                    android:layout_width="wrap_content"
                    android:layout_height="56dp"
                    android:background="@drawable/bg_focus_item"
                    android:clickable="true"
                    android:focusable="true"
                    android:gravity="center"
                    android:nextFocusUp="@id/btnSettings"
                    android:paddingHorizontal="24dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/startup_choose_provider"
                        android:textColor="@color/text_primary"
                        android:textStyle="bold"
                        android:textSize="@dimen/text_body_s" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>

        <!-- Home's own search entry point. The toolbar button is easy to miss from a sofa
             (it's a small target in the far corner), and Home is where someone arrives
             already knowing what they want to look for. Opens the same overlay. -->
        <LinearLayout
            android:id="@+id/homeSearchBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/bg_input"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp"
            android:paddingVertical="12dp"
            android:stateListAnimator="@animator/focus_scale_flat"
            android:visibility="gone">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginEnd="12dp"
                android:src="@drawable/ic_search"
                android:tint="@color/text_secondary" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/search_main_hint"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/text_subhead" />
        </LinearLayout>

        <!-- Home: continue watching / recently played / favorites shelves -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/homeContent"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:paddingHorizontal="16dp"
            android:visibility="gone" />

        <!-- Discover: TMDB browse/search for titles outside the provider catalog, played via a
             stream-search plugin. Its own pane (like Downloads) - no category sidebar. -->
        <LinearLayout
            android:id="@+id/discoverContent"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:visibility="gone">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="10dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- The field is a fake input: it's the discover search overlay's trigger,
                     not a real text box. Focusable + clickable so D-pad and touch both
                     reach it, with focus_scale_flat like the Home search bar; clicking it
                     opens dialog_discover_search.xml (the platform IME is never involved,
                     so the inner EditText stays non-focusable). The trailing chevron reads
                     as "opens something". -->
                <LinearLayout
                    android:id="@+id/discoverSearchField"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:background="@drawable/bg_input"
                    android:clickable="true"
                    android:focusable="true"
                    android:nextFocusUp="@id/tabDiscover"
                    android:gravity="center_vertical"
                    android:orientation="horizontal"
                    android:paddingHorizontal="14dp"
                    android:paddingVertical="12dp"
                    android:stateListAnimator="@animator/focus_scale_flat">

                    <ImageView
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_marginEnd="10dp"
                        android:src="@drawable/ic_search"
                        android:tint="@color/text_secondary" />

                    <EditText
                        android:id="@+id/discoverSearchInput"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:background="@null"
                        android:focusable="false"
                        android:focusableInTouchMode="false"
                        android:hint="@string/discover_search_hint"
                        android:imeOptions="actionSearch"
                        android:inputType="text"
                        android:maxLines="1"
                        android:textColor="@color/text_primary"
                        android:textColorHint="@color/text_secondary" />

                    <ImageView
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_marginStart="8dp"
                        android:contentDescription="@null"
                        android:src="@drawable/ic_chevron_right"
                        android:tint="@color/text_tertiary" />
                </LinearLayout>

                <TextView
                    android:id="@+id/discoverSearchButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:background="@drawable/card_surface_background"
                    android:clickable="true"
                    android:focusable="true"
                    android:nextFocusUp="@id/tabDiscover"
                    android:nextFocusDown="@id/discoverFilterSeries"
                    android:fontFamily="@font/inter_medium"
                    android:paddingHorizontal="20dp"
                    android:paddingVertical="12dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:text="@string/search"
                    android:textColor="@color/text_primary" />
            </LinearLayout>

            <!-- Type filter: Trending (default, also what a submitted search runs under),
                 Movies, Series - same season-chip lookalike as the search overlay's own
                 result-type row. Movies/Series switch the grid to TMDB's paginated Popular
                 ranking for that type instead of trending/week's thin mixed list (see
                 loadDiscoverByType). Selection is applied in code (isSelected), not XML. -->
            <LinearLayout
                android:id="@+id/discoverFilterRow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/discoverFilterAll"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="@dimen/search_chip_gap"
                    android:background="@drawable/bg_season_chip"
                    android:clickable="true"
                    android:focusable="true"
                    android:nextFocusUp="@id/discoverSearchField"
                    android:nextFocusLeft="@id/discoverFilterAll"
                    android:nextFocusRight="@id/discoverFilterMovies"
                    android:fontFamily="@font/inter_medium"
                    android:paddingHorizontal="18dp"
                    android:paddingVertical="10dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:text="@string/search_filter_all"
                    android:textColor="@color/season_chip_text"
                    android:textSize="@dimen/text_body_s" />

                <TextView
                    android:id="@+id/discoverFilterMovies"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="@dimen/search_chip_gap"
                    android:background="@drawable/bg_season_chip"
                    android:clickable="true"
                    android:focusable="true"
                    android:nextFocusUp="@id/discoverSearchField"
                    android:nextFocusLeft="@id/discoverFilterAll"
                    android:nextFocusRight="@id/discoverFilterSeries"
                    android:fontFamily="@font/inter_medium"
                    android:paddingHorizontal="18dp"
                    android:paddingVertical="10dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:text="@string/search_filter_films"
                    android:textColor="@color/season_chip_text"
                    android:textSize="@dimen/text_body_s" />

                <TextView
                    android:id="@+id/discoverFilterSeries"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_season_chip"
                    android:clickable="true"
                    android:focusable="true"
                    android:nextFocusUp="@id/discoverSearchButton"
                    android:nextFocusLeft="@id/discoverFilterMovies"
                    android:nextFocusRight="@id/discoverFilterSeries"
                    android:fontFamily="@font/inter_medium"
                    android:paddingHorizontal="18dp"
                    android:paddingVertical="10dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:text="@string/search_filter_series"
                    android:textColor="@color/season_chip_text"
                    android:textSize="@dimen/text_body_s" />
            </LinearLayout>

            <TextView
                android:id="@+id/discoverStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:textColor="@color/text_secondary"
                android:visibility="gone" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/discoverGrid"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:clipChildren="false"
                android:clipToPadding="false" />
        </LinearLayout>

        <!-- Settings: content added/removed at runtime by showProviderSettings(). Lives here
             (rather than covering the whole screen) so the toolbar + tab bar above stay
             visible and usable while Settings is open, same as every other tab. -->
        <FrameLayout
            android:id="@+id/settingsContainer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:visibility="gone" />

        <!-- Search: same content-slot pattern as Settings, same reason. -->
        <FrameLayout
            android:id="@+id/searchContainer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:visibility="gone" />

        <!-- Content row: category sidebar + active tab's list/grid. clipChildren false
             throughout - item_category rows scale to 1.06x on focus (focus_scale) and
             would otherwise get clipped top/bottom by categorySidebar's own bounds. -->
        <LinearLayout
            android:id="@+id/contentRow"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="horizontal">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/categorySidebar"
                android:layout_width="@dimen/category_sidebar_width"
                android:layout_height="match_parent"
                android:layout_marginStart="16dp"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/bg_category_panel"
                android:defaultFocusHighlightEnabled="true"
                android:padding="10dp"
                android:visibility="gone" />

            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1">

                <!-- Live TV row: full-width guide, preview pane floats on top of it in
                     the top-right corner instead of reserving a permanent side column -
                     the guide's program columns now run full width all the way down. -->
                <FrameLayout
                    android:id="@+id/liveRow"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:visibility="visible">

                    <!-- Guide runs full width by default; the preview pane floats on top in the
                         top-right corner instead of reserving a permanent side column. Rows
                         that are actually behind the preview get a right-side reservation added
                         at runtime (see updateGuideRowWrap() in MainActivity) so the guide reads
                         as text wrapping around the preview - narrow while a row overlaps it,
                         full width again for every row below it. -->
                    <!-- clipChildren/clipToPadding false: liveContent's rows are flush with
                         this layout's content box, so the focused channel-info card's 1.06x
                         scale-up grew straight into the 16dp padding and got clipped - the
                         focus stroke lost its left edge entirely. The RecyclerView already
                         disabled both flags; without them here too, its parent clipped what
                         it had allowed through. -->
                    <LinearLayout
                        android:id="@+id/liveGuideColumn"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:clipChildren="false"
                        android:clipToPadding="false"
                        android:orientation="vertical"
                        android:paddingHorizontal="16dp">

                        <!-- Guide time ruler: spacer matches the channel-info column width so
                             time labels line up with the program blocks, not the channel names. -->
                        <LinearLayout
                            android:layout_width="match_parent"
                            android:layout_height="22dp"
                            android:orientation="horizontal">

                            <!-- marginEnd matches guideChannelInfo's 10dp end margin in
                                 item_guide_channel_row.xml - without it every time label
                                 sits 10dp left of the programme column it labels. -->
                            <View
                                android:layout_width="@dimen/guide_channel_column_width"
                                android:layout_height="match_parent"
                                android:layout_marginEnd="10dp" />

                            <HorizontalScrollView
                                android:id="@+id/guideHeaderScroll"
                                android:layout_width="0dp"
                                android:layout_height="match_parent"
                                android:layout_weight="1"
                                android:overScrollMode="never"
                                android:scrollbars="none">

                                <LinearLayout
                                    android:id="@+id/guideHeaderRow"
                                    android:layout_width="wrap_content"
                                    android:layout_height="match_parent"
                                    android:orientation="horizontal" />
                            </HorizontalScrollView>
                        </LinearLayout>

                        <FrameLayout
                            android:layout_width="match_parent"
                            android:layout_height="0dp"
                            android:layout_weight="1"
                            android:clipChildren="false"
                            android:clipToPadding="false">

                            <androidx.recyclerview.widget.RecyclerView
                                android:id="@+id/liveContent"
                                android:layout_width="match_parent"
                                android:layout_height="match_parent"
                                android:layout_marginTop="4dp"
                                android:clipChildren="false"
                                android:clipToPadding="false"
                                android:defaultFocusHighlightEnabled="true"
                                android:paddingBottom="4dp" />

                            <FrameLayout
                                android:id="@+id/livePreviewGutter"
                                android:layout_width="@dimen/live_preview_width"
                                android:layout_height="wrap_content"
                                android:layout_gravity="top|end"
                                android:layout_marginTop="4dp"
                                android:visibility="gone">

                                <LinearLayout
                                    android:id="@+id/livePreviewPane"
                                    android:layout_width="match_parent"
                                    android:layout_height="wrap_content"
                                    android:layout_gravity="top"
                                    android:background="@drawable/bg_live_preview_frame"
                                    android:orientation="vertical"
                                    android:padding="2dp">

                                <FrameLayout
                                    android:layout_width="match_parent"
                                    android:layout_height="@dimen/live_preview_height">

                                    <TextureView
                                        android:id="@+id/previewSurface"
                                        android:layout_width="match_parent"
                                        android:layout_height="match_parent" />

                                    <ProgressBar
                                        android:id="@+id/previewBuffering"
                                        android:layout_width="28dp"
                                        android:layout_height="28dp"
                                        android:layout_gravity="center"
                                        android:indeterminate="true"
                                        android:indeterminateTint="@color/primary"
                                        android:visibility="gone" />
                                </FrameLayout>

                                <TextView
                                    android:id="@+id/previewChannelName"
                                    android:layout_width="match_parent"
                                    android:layout_height="wrap_content"
                                    android:layout_marginTop="6dp"
                                    android:ellipsize="end"
                                    android:gravity="center"
                                    android:maxLines="1"
                                    android:textColor="@color/text_primary"
                                    android:textSize="@dimen/text_body"
                                    android:textStyle="bold" />
                            </LinearLayout>
                        </FrameLayout>
                        </FrameLayout>
                    </LinearLayout>
                </FrameLayout>

                <!-- Content area: Series grid -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/seriesContent"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:defaultFocusHighlightEnabled="true"
                    android:paddingHorizontal="10dp"
                    android:paddingTop="8dp"
                    android:paddingBottom="8dp"
                    android:visibility="gone" />

                <!-- Content area: Films grid -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/filmsContent"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:defaultFocusHighlightEnabled="true"
                    android:paddingHorizontal="10dp"
                    android:paddingTop="8dp"
                    android:paddingBottom="8dp"
                    android:visibility="gone" />

                <!-- Content area: Catch Up. Columns rather than replaced screens: the
                     channel stays on the left while the days fly out beside it and that
                     day's programmes beside those, so the whole path you took is on screen
                     at once and stepping back is a LEFT press rather than a redraw. -->
                <LinearLayout
                    android:id="@+id/catchupContent"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical"
                    android:visibility="gone">

                    <TextView
                        android:id="@+id/catchupCrumb"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:fontFamily="@font/inter_semibold"
                        android:maxLines="1"
                        android:paddingHorizontal="16dp"
                        android:paddingTop="8dp"
                        android:paddingBottom="6dp"
                        android:textColor="@color/text_primary"
                        android:textSize="@dimen/text_body_s" />

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        android:layout_weight="1"
                        android:orientation="horizontal">

                        <!-- Categories: the entry column. Provider category names, so a
                             220-channel archive is browsable without scrolling past 200
                             channels to reach the one you want. -->
                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/catchupCategoryList"
                            android:layout_width="@dimen/catchup_category_column_width"
                            android:layout_height="match_parent"
                            android:clipChildren="false"
                            android:clipToPadding="false"
                            android:defaultFocusHighlightEnabled="true"
                            android:paddingHorizontal="8dp"
                            android:paddingBottom="8dp" />

                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/catchupChannelList"
                            android:layout_width="@dimen/catchup_channel_column_width"
                            android:layout_height="match_parent"
                            android:clipChildren="false"
                            android:clipToPadding="false"
                            android:defaultFocusHighlightEnabled="true"
                            android:paddingHorizontal="8dp"
                            android:paddingBottom="8dp"
                            android:visibility="gone" />

                        <!-- Days: flies out beside the channel once one is picked. -->
                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/catchupDayList"
                            android:layout_width="@dimen/catchup_day_column_width"
                            android:layout_height="match_parent"
                            android:clipChildren="false"
                            android:clipToPadding="false"
                            android:defaultFocusHighlightEnabled="true"
                            android:paddingHorizontal="8dp"
                            android:paddingBottom="8dp"
                            android:visibility="gone" />

                        <!-- Programmes: the widest column, since its rows carry a title and
                             a time range. Holds its own status line (loading / no guide). -->
                        <LinearLayout
                            android:id="@+id/catchupProgrammeColumn"
                            android:layout_width="0dp"
                            android:layout_height="match_parent"
                            android:layout_weight="1"
                            android:orientation="vertical"
                            android:visibility="gone">

                            <TextView
                                android:id="@+id/catchupStatus"
                                android:layout_width="match_parent"
                                android:layout_height="wrap_content"
                                android:paddingHorizontal="14dp"
                                android:paddingBottom="6dp"
                                android:textColor="@color/text_secondary"
                                android:textSize="@dimen/text_caption"
                                android:visibility="gone" />

                            <androidx.recyclerview.widget.RecyclerView
                                android:id="@+id/catchupProgrammeList"
                                android:layout_width="match_parent"
                                android:layout_height="0dp"
                                android:layout_weight="1"
                                android:clipChildren="false"
                                android:clipToPadding="false"
                                android:defaultFocusHighlightEnabled="true"
                                android:paddingHorizontal="8dp"
                                android:paddingBottom="8dp" />
                        </LinearLayout>
                    </LinearLayout>
                </LinearLayout>

                <!-- Content area: Downloads list (mobile only) -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/downloadsContent"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:defaultFocusHighlightEnabled="true"
                    android:paddingHorizontal="16dp"
                    android:paddingBottom="4dp"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/downloadsEmptyText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="@string/no_downloads_yet"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/text_body_s"
                    android:visibility="gone" />

                <!-- Collapse re-expand affordance (phone-only): when the category sidebar is
                     collapsed, this is the only way back to the categories, so it is a
                     labelled pill rather than a bare icon - an unlabelled grey chevron
                     floating over the content reads as decoration and users concluded the
                     collapse was one-way. Anchored top-start (where the rail's own top-left
                     corner was, and where the eye looks for navigation) rather than floating
                     at mid-height. Drawn last so it overlays the content list; visibility is
                     driven by applySidebarVisibility() alongside the rail's own, never by
                     hand. Focusable so a D-pad user on the content's first row can reach it
                     with one LEFT press. -->
                <LinearLayout
                    android:id="@+id/sidebarExpandButton"
                    android:layout_width="wrap_content"
                    android:layout_height="48dp"
                    android:layout_gravity="start|top"
                    android:layout_marginStart="16dp"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_sidebar_expand_pill"
                    android:clickable="true"
                    android:contentDescription="@string/show_categories"
                    android:focusable="true"
                    android:gravity="center_vertical"
                    android:orientation="horizontal"
                    android:paddingStart="14dp"
                    android:paddingEnd="12dp"
                    android:stateListAnimator="@animator/focus_scale_flat"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/categories"
                        android:textColor="@color/text_primary"
                        android:textSize="@dimen/text_body_s"
                        android:textStyle="bold" />

                    <ImageView
                        android:layout_width="18dp"
                        android:layout_height="18dp"
                        android:layout_marginStart="6dp"
                        android:contentDescription="@null"
                        android:src="@drawable/ic_chevron_right"
                        android:tint="@color/text_primary" />
                </LinearLayout>

            </FrameLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- ============================================================
         CONTENT DETAIL (fullscreen "you're now on this film's page",
         not a popup - opened from Series/Films/Home)
         ============================================================ -->
    <FrameLayout
        android:id="@+id/contentDetailLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/surface"
        android:visibility="gone">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:orientation="vertical">

                <!-- Compact poster (2:3, not a full-width landscape banner) sits beside the
                     title/buttons instead of above them - frees up most of the vertical
                     space a hero banner used to take, so far less scrolling is needed to
                     reach the plot/cast/episodes below, and D-pad "scroll down" reaches
                     them in far fewer presses. -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:paddingHorizontal="24dp"
                    android:paddingTop="72dp">

                    <FrameLayout
                        android:layout_width="120dp"
                        android:layout_height="180dp"
                        android:background="@drawable/bg_rounded_poster"
                        android:clipToOutline="true"
                        android:elevation="4dp">

                        <ImageView
                            android:id="@+id/detailBackdrop"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:scaleType="centerCrop" />
                    </FrameLayout>

                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="16dp"
                        android:layout_weight="1"
                        android:orientation="vertical">

                        <TextView
                            android:id="@+id/detailTitle"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:ellipsize="end"
                            android:fontFamily="@font/inter_semibold"
                            android:maxLines="2"
                            android:textColor="@color/text_primary"
                            android:textSize="@dimen/text_title" />

                        <TextView
                            android:id="@+id/detailMeta"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="10dp"
                            android:background="@drawable/pill_number"
                            android:ellipsize="end"
                            android:fontFamily="@font/inter_semibold"
                            android:maxLines="1"
                            android:paddingHorizontal="10dp"
                            android:paddingVertical="5dp"
                            android:textColor="@color/text_secondary"
                            android:textSize="@dimen/text_caption" />

                        <TextView
                            android:id="@+id/detailReleaseDate"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="10dp"
                            android:ellipsize="end"
                            android:fontFamily="@font/inter_regular"
                            android:maxLines="1"
                            android:textColor="@color/text_tertiary"
                            android:textSize="@dimen/text_caption"
                            android:visibility="gone" />

                        <TextView
                            android:id="@+id/detailPlotLabel"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="14dp"
                            android:fontFamily="@font/inter_semibold"
                            android:text="@string/overview"
                            android:textAllCaps="true"
                            android:textColor="@color/text_secondary"
                            android:textSize="@dimen/text_tiny"
                            android:visibility="gone" />

                        <TextView
                            android:id="@+id/detailPlot"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="6dp"
                            android:ellipsize="end"
                            android:fontFamily="@font/inter_regular"
                            android:lineSpacingExtra="3dp"
                            android:maxLines="5"
                            android:textColor="@color/text_secondary"
                            android:textSize="@dimen/text_caption"
                            android:visibility="gone" />

                        <TextView
                            android:id="@+id/detailCastLabel"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="12dp"
                            android:fontFamily="@font/inter_semibold"
                            android:text="@string/cast"
                            android:textAllCaps="true"
                            android:textColor="@color/text_secondary"
                            android:textSize="@dimen/text_tiny"
                            android:visibility="gone" />

                        <TextView
                            android:id="@+id/detailCast"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="6dp"
                            android:ellipsize="end"
                            android:fontFamily="@font/inter_regular"
                            android:maxLines="3"
                            android:textColor="@color/text_tertiary"
                            android:textSize="@dimen/text_caption"
                            android:visibility="gone" />
                    </LinearLayout>
                </LinearLayout>

                <!-- clipChildren/clipToPadding false: the episode rows and buttons below all
                     scale to 1.06x on focus, and this container's 24dp horizontal padding
                     would otherwise slice the scaled-up row off at both edges. -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:orientation="vertical"
                    android:paddingHorizontal="24dp"
                    android:paddingBottom="24dp">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="20dp"
                        android:clipChildren="false"
                        android:clipToPadding="false"
                        android:orientation="horizontal">

                        <LinearLayout
                            android:id="@+id/detailPlayButton"
                            android:layout_width="wrap_content"
                            android:layout_height="48dp"
                            android:background="@drawable/bg_play_button"
                            android:clickable="true"
                            android:focusable="true"
                            android:gravity="center"
                            android:orientation="horizontal"
                            android:paddingHorizontal="28dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:visibility="gone">

                            <ImageView
                                android:layout_width="18dp"
                                android:layout_height="18dp"
                                android:src="@drawable/ic_play"
                                android:tint="@android:color/white" />

                            <TextView
                                android:id="@+id/detailPlayButtonLabel"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:layout_marginStart="8dp"
                                android:fontFamily="@font/inter_semibold"
                                android:text="@string/play"
                                android:textAllCaps="true"
                                android:textColor="@android:color/white"
                                android:textSize="@dimen/text_body" />
                        </LinearLayout>

                        <LinearLayout
                            android:id="@+id/detailFavoriteButton"
                            android:layout_width="48dp"
                            android:layout_height="48dp"
                            android:layout_marginStart="10dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:focusable="true"
                            android:gravity="center"
                            android:stateListAnimator="@animator/focus_scale"
                            android:visibility="gone">

                            <TextView
                                android:id="@+id/detailFavoriteIcon"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="☆"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_title" />
                        </LinearLayout>

                        <LinearLayout
                            android:id="@+id/detailDownloadButton"
                            android:layout_width="48dp"
                            android:layout_height="48dp"
                            android:layout_marginStart="10dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:focusable="true"
                            android:gravity="center"
                            android:stateListAnimator="@animator/focus_scale"
                            android:visibility="gone">

                            <ImageView
                                android:id="@+id/detailDownloadIcon"
                                android:layout_width="20dp"
                                android:layout_height="20dp"
                                android:src="@android:drawable/stat_sys_download"
                                android:tint="@color/text_primary" />
                        </LinearLayout>

                        <!-- Shown only when a stream_search plugin is installed+enabled (see
                             wireFindStreamButton). Hands off to a companion plugin to find a
                             playable stream for this title. -->
                        <LinearLayout
                            android:id="@+id/detailFindStreamButton"
                            android:layout_width="wrap_content"
                            android:layout_height="48dp"
                            android:layout_marginStart="10dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:focusable="true"
                            android:gravity="center"
                            android:orientation="horizontal"
                            android:paddingHorizontal="18dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:visibility="gone">

                            <TextView
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:fontFamily="@font/inter_medium"
                                android:text="@string/find_stream"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_body" />
                        </LinearLayout>

                        <!-- Shown only when TMDB has a trailer for this title (see
                             wireTrailerButton). Opens a YouTube /embed player in-app. -->
                        <LinearLayout
                            android:id="@+id/detailTrailerButton"
                            android:layout_width="wrap_content"
                            android:layout_height="48dp"
                            android:layout_marginStart="10dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:focusable="true"
                            android:gravity="center"
                            android:orientation="horizontal"
                            android:paddingHorizontal="18dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:visibility="gone">

                            <TextView
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:fontFamily="@font/inter_medium"
                                android:text="@string/trailer"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_body" />
                        </LinearLayout>
                    </LinearLayout>

                    <!-- clipChildren/clipToPadding false: item_category chips inside scale to
                         1.06x on focus and would otherwise be clipped top/bottom by these
                         wrap_content containers whose bounds hug the chip height exactly. -->
                    <HorizontalScrollView
                        android:id="@+id/detailVersionsScroll"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:clipChildren="false"
                        android:clipToPadding="false"
                        android:scrollbars="none"
                        android:visibility="gone">

                        <LinearLayout
                            android:id="@+id/detailVersionsRow"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:clipChildren="false"
                            android:clipToPadding="false"
                            android:orientation="horizontal" />
                    </HorizontalScrollView>

                    <View
                        android:layout_width="match_parent"
                        android:layout_height="1dp"
                        android:layout_marginTop="20dp"
                        android:background="@color/divider" />

                    <TextView
                        android:id="@+id/detailSectionLabel"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="18dp"
                        android:fontFamily="@font/inter_semibold"
                        android:textAllCaps="true"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/text_caption" />

                    <HorizontalScrollView
                        android:id="@+id/detailSeasonScroll"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:clipChildren="false"
                        android:clipToPadding="false"
                        android:scrollbars="none"
                        android:visibility="gone">

                        <LinearLayout
                            android:id="@+id/detailSeasonRow"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:clipChildren="false"
                            android:clipToPadding="false"
                            android:orientation="horizontal" />
                    </HorizontalScrollView>

                    <TextView
                        android:id="@+id/detailStatus"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:gravity="center"
                        android:text="@string/loading"
                        android:textColor="@color/text_secondary"
                        android:textSize="@dimen/text_body_s" />

                    <!-- marginEnd is 0dp off-TV; on TV it pulls the rows in from the full
                         panel width, which was far wider than a row's actual content. -->
                    <!-- FullHeightRecyclerView, not a plain RecyclerView: inside a ScrollView a
                         plain one measures itself to a single row. See that class. -->
                    <com.lumora.view.FullHeightRecyclerView
                        android:id="@+id/detailItemsList"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="6dp"
                        android:layout_marginEnd="@dimen/detail_list_end_inset"
                        android:clipChildren="false"
                        android:clipToPadding="false"
                        android:nestedScrollingEnabled="false"
                        android:overScrollMode="never"
                        android:visibility="gone" />
                </LinearLayout>
            </LinearLayout>
        </ScrollView>

        <!-- Floats outside the ScrollView so it never scrolls away and costs no
             vertical space in the flow above. -->
        <ImageButton
            android:id="@+id/detailBackButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="top|start"
            android:layout_margin="16dp"
            android:background="@drawable/bg_toolbar_button"
            android:clickable="true"
            android:contentDescription="Back"
            android:elevation="8dp"
            android:focusable="true"
            android:padding="12dp"
            android:stateListAnimator="@animator/focus_scale"
            android:src="@drawable/ic_x"
            android:tint="@color/text_primary" />
    </FrameLayout>

    <!-- ============================================================
         PLAYER OVERLAY (with D-pad friendly controls)
         ============================================================ -->
    <FrameLayout
        android:id="@+id/playerLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:visibility="gone">

        <com.lumora.player.VideoAspectFrameLayout
            android:id="@+id/playerAspectContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black">

            <SurfaceView
                android:id="@+id/playerSurface"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />
        </com.lumora.player.VideoAspectFrameLayout>

        <androidx.media3.ui.SubtitleView
            android:id="@+id/playerSubtitleView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="false"
            android:focusable="false" />

        <ProgressBar
            android:id="@+id/bufferingSpinner"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:indeterminateTint="@color/primary"
            android:visibility="gone" />

        <!-- Numeric channel entry overlay - anchored top-center, deliberately clear of
             bufferingSpinner (dead center) so keying in a channel while the previous one
             is still buffering doesn't render both stacked on top of each other. -->
        <LinearLayout
            android:id="@+id/numericInputOverlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|center_horizontal"
            android:layout_marginTop="48dp"
            android:background="@drawable/bg_player_bottom"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/numericInputDigits"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="@color/text_primary"
                android:textSize="@dimen/text_hero"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/numericInputChannelName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/text_subhead"
                android:visibility="gone" />
        </LinearLayout>

        <!-- Controls Overlay: gradient scrims top/bottom, no full-screen dim -->
        <LinearLayout
            android:id="@+id/controlsOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:animateLayoutChanges="true"
            android:background="@android:color/transparent"
            android:orientation="vertical"
            android:visibility="visible">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:background="@drawable/grad_player_top"
                android:paddingHorizontal="16dp">
                <ImageButton
                    android:id="@+id/btnBack"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_centerVertical="true"
                    android:background="@drawable/bg_toolbar_button"
                    android:clickable="true"
                    android:contentDescription="Back"
                    android:focusable="true"
                    android:padding="12dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:src="@drawable/ic_x"
                    android:tint="@color/text_primary" />
                <!-- Phone access to the side menu - TV remotes open it with DPAD_LEFT, so this
                     is hidden on TV (see setupPlayerControls). -->
                <ImageButton
                    android:id="@+id/btnPlayerMenu"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_centerVertical="true"
                    android:layout_marginStart="12dp"
                    android:layout_toEndOf="@id/btnBack"
                    android:background="@drawable/bg_toolbar_button"
                    android:clickable="true"
                    android:contentDescription="@string/player_menu"
                    android:focusable="true"
                    android:padding="12dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:src="@drawable/ic_menu"
                    android:tint="@color/text_primary" />
            </RelativeLayout>

            <Space android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1" />

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/grad_player_bottom"
                android:paddingTop="10dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_margin="6dp"
                    android:background="@drawable/bg_player_bottom"
                    android:orientation="vertical"
                    android:paddingHorizontal="12dp"
                    android:paddingVertical="4dp">

                    <!-- Info card: logo + program title/time on the left, channel name + badges on the right -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:gravity="center_vertical"
                        android:orientation="horizontal">

                        <FrameLayout
                            android:id="@+id/playerLogoBox"
                            android:layout_width="36dp"
                            android:layout_height="36dp"
                            android:background="@drawable/shape_logo_box"
                            android:clipToOutline="true"
                            android:elevation="2dp">

                            <TextView
                                android:id="@+id/playerChannelInitial"
                                android:layout_width="match_parent"
                                android:layout_height="match_parent"
                                android:gravity="center"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_subhead"
                                android:textStyle="bold" />

                            <ImageView
                                android:id="@+id/playerChannelLogo"
                                android:layout_width="match_parent"
                                android:layout_height="match_parent"
                                android:padding="6dp"
                                android:scaleType="fitCenter"
                                android:visibility="gone" />
                        </FrameLayout>

                        <LinearLayout
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="12dp"
                            android:layout_weight="1"
                            android:orientation="vertical">

                            <TextView
                                android:id="@+id/playerChannelName"
                                android:layout_width="match_parent"
                                android:layout_height="wrap_content"
                                android:ellipsize="end"
                                android:maxLines="1"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_subhead"
                                android:textStyle="bold" />

                            <TextView
                                android:id="@+id/playerSubtitle"
                                android:layout_width="match_parent"
                                android:layout_height="wrap_content"
                                android:layout_marginTop="2dp"
                                android:ellipsize="end"
                                android:maxLines="1"
                                android:textColor="@color/text_secondary"
                                android:textSize="@dimen/text_caption"
                                android:visibility="gone" />
                        </LinearLayout>

                        <TextView
                            android:id="@+id/playerLiveBadge"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="8dp"
                            android:background="@drawable/pill_live"
                            android:paddingHorizontal="10dp"
                            android:paddingVertical="4dp"
                            android:text="@string/live_badge"
                            android:textAllCaps="true"
                            android:textColor="@android:color/white"
                            android:textSize="@dimen/text_tiny"
                            android:textStyle="bold" />
                    </LinearLayout>

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="6dp"
                        android:gravity="center"
                        android:orientation="horizontal">
                        <ImageButton
                            android:id="@+id/btnPrevChannel"
                            android:layout_width="38dp"
                            android:layout_height="38dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:contentDescription="Previous"
                            android:focusable="true"
                            android:nextFocusRight="@id/btnRewind"
                            android:nextFocusDown="@id/seekBar"
                            android:padding="10dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:src="@android:drawable/ic_media_previous"
                            android:tint="@color/text_primary" />
                        <ImageButton
                            android:id="@+id/btnRewind"
                            android:layout_width="38dp"
                            android:layout_height="38dp"
                            android:layout_marginEnd="6dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:contentDescription="Rewind 15s"
                            android:focusable="true"
                            android:nextFocusLeft="@id/btnPrevChannel"
                            android:nextFocusRight="@id/btnPlayPause"
                            android:nextFocusDown="@id/seekBar"
                            android:padding="8dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:src="@android:drawable/ic_media_rew"
                            android:tint="@color/text_primary" />
                        <ImageButton
                            android:id="@+id/btnPlayPause"
                            android:layout_width="46dp"
                            android:layout_height="46dp"
                            android:layout_marginHorizontal="10dp"
                            android:background="@drawable/bg_play_button"
                            android:clickable="true"
                            android:contentDescription="Play/Pause"
                            android:focusable="true"
                            android:nextFocusLeft="@id/btnRewind"
                            android:nextFocusRight="@id/btnFastForward"
                            android:nextFocusDown="@id/seekBar"
                            android:padding="11dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:src="@drawable/ic_play"
                            android:tint="@android:color/white" />
                        <ImageButton
                            android:id="@+id/btnFastForward"
                            android:layout_width="38dp"
                            android:layout_height="38dp"
                            android:layout_marginStart="6dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:contentDescription="Fast-forward 30s"
                            android:focusable="true"
                            android:nextFocusLeft="@id/btnPlayPause"
                            android:nextFocusRight="@id/btnNextChannel"
                            android:nextFocusDown="@id/seekBar"
                            android:padding="8dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:src="@android:drawable/ic_media_ff"
                            android:tint="@color/text_primary" />
                        <ImageButton
                            android:id="@+id/btnNextChannel"
                            android:layout_width="38dp"
                            android:layout_height="38dp"
                            android:background="@drawable/bg_toolbar_button"
                            android:clickable="true"
                            android:contentDescription="Next"
                            android:focusable="true"
                            android:nextFocusLeft="@id/btnFastForward"
                            android:nextFocusDown="@id/seekBar"
                            android:padding="12dp"
                            android:stateListAnimator="@animator/focus_scale"
                            android:src="@android:drawable/ic_media_next"
                            android:tint="@color/text_primary" />
                    </LinearLayout>

                    <!-- Seek-preview thumbnail (Jellyfin trickplay tiles). Sits above the
                         seek row, hidden until a scrub actually has a frame to show. -->
                    <ImageView
                        android:id="@+id/trickplayPreview"
                        android:layout_width="176dp"
                        android:layout_height="99dp"
                        android:layout_gravity="center_horizontal"
                        android:layout_marginBottom="4dp"
                        android:background="@drawable/card_surface_background"
                        android:contentDescription="@string/seek_preview"
                        android:padding="2dp"
                        android:scaleType="fitCenter"
                        android:visibility="gone" />

                    <FrameLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="2dp">
                        <TextView
                            android:id="@+id/currentTime"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="start|center_vertical"
                            android:text="0:00"
                            android:textColor="@color/text_secondary"
                            android:textSize="@dimen/text_caption" />
                        <SeekBar
                            android:id="@+id/seekBar"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:nextFocusUp="@id/btnPlayPause"
                            android:nextFocusDown="@id/btnSpeed"
                            android:paddingStart="48dp"
                            android:paddingEnd="48dp"
                            android:progressTint="@color/primary"
                            android:progressBackgroundTint="@color/surface_accent"
                            android:thumbTint="@color/focus" />
                        <TextView
                            android:id="@+id/duration"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="end|center_vertical"
                            android:text="0:00"
                            android:textColor="@color/text_secondary"
                            android:textSize="@dimen/text_caption" />
                    </FrameLayout>

                    <HorizontalScrollView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="4dp"
                        android:overScrollMode="never"
                        android:scrollbars="none">

                        <LinearLayout
                            android:id="@+id/playerTrackButtons"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:gravity="center_vertical"
                            android:orientation="horizontal">

                            <TextView
                                android:id="@+id/btnSpeed"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusRight="@id/btnAudioOffset"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/speed_default"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <TextView
                                android:id="@+id/btnAudioOffset"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnSpeed"
                                android:nextFocusRight="@id/btnSleepTimer"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/play_audio_offset_default"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <TextView
                                android:id="@+id/btnSleepTimer"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnAudioOffset"
                                android:nextFocusRight="@id/btnCast"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/sleep_timer_button"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <androidx.mediarouter.app.MediaRouteButton
                                android:id="@+id/btnCast"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginHorizontal="2dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:focusable="true"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnSleepTimer"
                                android:nextFocusRight="@id/btnExternalPlayer"
                                android:paddingHorizontal="4dp"
                                android:stateListAnimator="@animator/focus_scale" />

                            <!-- Hands the stream to VLC/MX Player/whatever is installed, at the
                                 current position (see MainActivityExternalPlayer.kt). Sits next
                                 to Diagnostics because both are "this isn't playing right"
                                 tools. Long-press sets the default player. -->
                            <TextView
                                android:id="@+id/btnExternalPlayer"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnCast"
                                android:nextFocusRight="@id/btnDiagnostics"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/open_in_external_player_short"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <TextView
                                android:id="@+id/btnDiagnostics"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnExternalPlayer"
                                android:nextFocusRight="@id/btnRecord"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/diag"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <TextView
                                android:id="@+id/btnRecord"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnDiagnostics"
                                android:nextFocusRight="@id/btnLiveVersions"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/rec"
                                android:textAllCaps="true"
                                android:textColor="@color/live_red"
                                android:textSize="@dimen/text_tiny"
                                android:textStyle="bold" />

                            <TextView
                                android:id="@+id/btnLiveVersions"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnRecord"
                                android:nextFocusRight="@id/btnAspectRatio"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/versions"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny"
                                android:visibility="gone" />

                            <TextView
                                android:id="@+id/btnAspectRatio"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnLiveVersions"
                                android:nextFocusRight="@id/btnAudioTrack"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/fit"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <TextView
                                android:id="@+id/btnAudioTrack"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnAspectRatio"
                                android:nextFocusRight="@id/btnSubtitleTrack"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/audio"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <TextView
                                android:id="@+id/btnSubtitleTrack"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:layout_marginEnd="4dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnAudioTrack"
                                android:nextFocusRight="@id/btnChapters"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/subtitles"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny" />

                            <!-- Only shown for an item the server actually has chapters for
                                 (Jellyfin or Plex) - see updateChaptersButtonVisibility. -->
                            <TextView
                                android:id="@+id/btnChapters"
                                android:layout_width="wrap_content"
                                android:layout_height="30dp"
                                android:background="@drawable/bg_toolbar_button"
                                android:clickable="true"
                                android:focusable="true"
                                android:gravity="center"
                                android:nextFocusUp="@id/seekBar"
                                android:nextFocusLeft="@id/btnSubtitleTrack"
                                android:paddingHorizontal="8dp"
                                android:stateListAnimator="@animator/focus_scale"
                                android:text="@string/chapters"
                                android:textAllCaps="true"
                                android:textColor="@color/text_primary"
                                android:textSize="@dimen/text_tiny"
                                android:visibility="gone" />
                        </LinearLayout>
                    </HorizontalScrollView>
                </LinearLayout>
            </FrameLayout>
        </LinearLayout>
        <!-- Player side menu: app navigation drawer that slides in from the left edge over
             the still-playing video (translucent panel - the video reads through). Opened by
             DPAD_LEFT (TV) or the btnPlayerMenu hamburger (phone); closed by BACK / DPAD_RIGHT
             / the X button / picking a destination. Drawn after controlsOverlay so it layers
             above the bottom bar when both exist.

             Two columns side by side: the fixed-width nav column, and the category column
             that flies out to its RIGHT when a section row is expanded (width 0 while
             collapsed, so the panel itself is nav-width until then). The panel is
             wrap_content horizontally so its right edge - and the rounded corners +
             drop shadow on it - travels with the category column as it opens. -->
        <LinearLayout
            android:id="@+id/playerSideMenu"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@drawable/bg_player_side_menu"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:elevation="8dp"
            android:orientation="horizontal"
            android:outlineProvider="background"
            android:visibility="gone">

        <LinearLayout
            android:id="@+id/sideMenuNavColumn"
            android:layout_width="@dimen/player_side_menu_width"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="vertical"
            android:padding="8dp">

            <!-- Header: title + close affordance (phone touch / TV both) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:focusable="false"
                    android:text="@string/player_menu_title"
                    android:textAllCaps="true"
                    android:textColor="@color/text_primary"
                    android:textSize="@dimen/text_caption"
                    android:textStyle="bold" />

                <ImageButton
                    android:id="@+id/btnCloseSideMenu"
                    android:layout_width="@dimen/side_menu_row_height"
                    android:layout_height="@dimen/side_menu_row_height"
                    android:background="@drawable/bg_toolbar_button"
                    android:clickable="true"
                    android:contentDescription="@string/close_side_menu"
                    android:focusable="true"
                    android:nextFocusDown="@id/navHome"
                    android:nextFocusUp="@id/navSettings"
                    android:padding="10dp"
                    android:src="@drawable/ic_x"
                    android:stateListAnimator="@animator/focus_scale"
                    android:tint="@color/text_primary" />
            </LinearLayout>

            <!-- App navigation: the same destinations as the top chrome (Home / Live TV /
                 Series / Movies / Discover, + Downloads on phones, + Settings). The
                 currently-open section's row carries the selected state (brand_muted fill +
                 primary border via bg_select_item), set in Kotlin when the menu opens and
                 cleared on close. Focus chain: close -> Home -> Live -> Series -> Movies ->
                 Discover -> Downloads -> Settings -> close (wraps). Every row uses the
                 side_menu_* metrics so the whole list fits without scrolling. -->
            <LinearLayout
                android:id="@+id/navHome"
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:layout_marginTop="2dp"
                android:background="@drawable/bg_select_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:nextFocusUp="@id/btnCloseSideMenu"
                android:nextFocusDown="@id/navLive"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:stateListAnimator="@animator/focus_scale">

                <ImageView
                    android:layout_width="@dimen/side_menu_icon"
                    android:layout_height="@dimen/side_menu_icon"
                    android:src="@drawable/ic_home"
                    android:tint="@color/text_secondary" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:fontFamily="@font/inter_medium"
                    android:maxLines="1"
                    android:text="@string/tab_home"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/side_menu_text" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/navLive"
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:layout_marginTop="2dp"
                android:background="@drawable/bg_select_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:nextFocusUp="@id/navHome"
                android:nextFocusDown="@id/navSeries"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:stateListAnimator="@animator/focus_scale">

                <ImageView
                    android:layout_width="@dimen/side_menu_icon"
                    android:layout_height="@dimen/side_menu_icon"
                    android:src="@drawable/ic_live_tv"
                    android:tint="@color/text_secondary" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:fontFamily="@font/inter_medium"
                    android:maxLines="1"
                    android:text="@string/tab_live_tv"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/side_menu_text" />

                <!-- Expand affordance: points right at the column this row opens. -->
                <ImageView
                    android:id="@+id/navLiveChevron"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginStart="6dp"
                    android:importantForAccessibility="no"
                    android:src="@drawable/ic_chevron_right"
                    android:tint="@color/text_tertiary" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/navSeries"
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:layout_marginTop="2dp"
                android:background="@drawable/bg_select_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:nextFocusUp="@id/navLive"
                android:nextFocusDown="@id/navFilms"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:stateListAnimator="@animator/focus_scale">

                <ImageView
                    android:layout_width="@dimen/side_menu_icon"
                    android:layout_height="@dimen/side_menu_icon"
                    android:src="@drawable/ic_series"
                    android:tint="@color/text_secondary" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:fontFamily="@font/inter_medium"
                    android:maxLines="1"
                    android:text="@string/series_tab"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/side_menu_text" />

                <ImageView
                    android:id="@+id/navSeriesChevron"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginStart="6dp"
                    android:importantForAccessibility="no"
                    android:src="@drawable/ic_chevron_right"
                    android:tint="@color/text_tertiary" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/navFilms"
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:layout_marginTop="2dp"
                android:background="@drawable/bg_select_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:nextFocusUp="@id/navSeries"
                android:nextFocusDown="@id/navDiscover"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:stateListAnimator="@animator/focus_scale">

                <ImageView
                    android:layout_width="@dimen/side_menu_icon"
                    android:layout_height="@dimen/side_menu_icon"
                    android:src="@drawable/ic_films"
                    android:tint="@color/text_secondary" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:fontFamily="@font/inter_medium"
                    android:maxLines="1"
                    android:text="@string/films_tab"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/side_menu_text" />

                <ImageView
                    android:id="@+id/navFilmsChevron"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginStart="6dp"
                    android:importantForAccessibility="no"
                    android:src="@drawable/ic_chevron_right"
                    android:tint="@color/text_tertiary" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/navDiscover"
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:layout_marginTop="2dp"
                android:background="@drawable/bg_select_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:nextFocusUp="@id/navFilms"
                android:nextFocusDown="@id/navDownloads"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:stateListAnimator="@animator/focus_scale">

                <ImageView
                    android:layout_width="@dimen/side_menu_icon"
                    android:layout_height="@dimen/side_menu_icon"
                    android:src="@drawable/ic_discover"
                    android:tint="@color/text_secondary" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:fontFamily="@font/inter_medium"
                    android:maxLines="1"
                    android:text="@string/discover_tab"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/side_menu_text" />
            </LinearLayout>

            <!-- Downloads is a mobile-only section (mirrors tabDownloads; stays GONE on TV).
                 While gone, Discover's nextFocusDown is retargeted to Settings in Kotlin. -->
            <LinearLayout
                android:id="@+id/navDownloads"
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:layout_marginTop="2dp"
                android:background="@drawable/bg_select_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:nextFocusUp="@id/navDiscover"
                android:nextFocusDown="@id/navSettings"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:stateListAnimator="@animator/focus_scale"
                android:visibility="gone">

                <ImageView
                    android:layout_width="@dimen/side_menu_icon"
                    android:layout_height="@dimen/side_menu_icon"
                    android:src="@drawable/ic_downloads"
                    android:tint="@color/text_secondary" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:fontFamily="@font/inter_medium"
                    android:maxLines="1"
                    android:text="@string/downloads_tab"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/side_menu_text" />
            </LinearLayout>

            <!-- Settings: same destination as the browse screen's gear button, which the
                 player covers while it's up. -->
            <LinearLayout
                android:id="@+id/navSettings"
                android:layout_width="match_parent"
                android:layout_height="@dimen/side_menu_row_height"
                android:layout_marginTop="2dp"
                android:background="@drawable/bg_select_item"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:nextFocusUp="@id/navDownloads"
                android:nextFocusDown="@id/btnCloseSideMenu"
                android:orientation="horizontal"
                android:paddingHorizontal="10dp"
                android:stateListAnimator="@animator/focus_scale">

                <ImageView
                    android:layout_width="@dimen/side_menu_icon"
                    android:layout_height="@dimen/side_menu_icon"
                    android:src="@drawable/ic_settings"
                    android:tint="@color/text_secondary" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:fontFamily="@font/inter_medium"
                    android:maxLines="1"
                    android:text="@string/settings"
                    android:textColor="@color/text_secondary"
                    android:textSize="@dimen/side_menu_text" />
            </LinearLayout>
        </LinearLayout>

            <!-- Category drill-down for the current-tab section row (Live/Series/Films) -
                 the second column, right of the nav rows, which keep their place. The
                 container clips, so animating ITS width 0 -> player_side_menu_category_width
                 wipes the fixed-width list into view left-to-right (rather than reflowing
                 the rows every frame, which animating the RecyclerView itself would do).
                 Starts GONE at width 0 - expansion is owned entirely by MainActivity. -->
            <FrameLayout
                android:id="@+id/sideMenuCategoryPanel"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:visibility="gone">

                <View
                    android:layout_width="1dp"
                    android:layout_height="match_parent"
                    android:layout_gravity="start"
                    android:background="@color/color_border" />

                <!-- Fixed width so nothing inside reflows while the container animates. -->
                <LinearLayout
                    android:layout_width="@dimen/player_side_menu_category_width"
                    android:layout_height="match_parent"
                    android:layout_gravity="start"
                    android:orientation="vertical"
                    android:paddingStart="4dp"
                    android:paddingTop="8dp"
                    android:paddingEnd="4dp"
                    android:paddingBottom="8dp">

                    <!-- Which level the column is showing: the section's categories, or one
                         category's channels/titles after drilling a step further right. -->
                    <TextView
                        android:id="@+id/sideMenuColumnTitle"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/side_menu_row_height"
                        android:ellipsize="end"
                        android:focusable="false"
                        android:gravity="center_vertical"
                        android:maxLines="1"
                        android:paddingHorizontal="12dp"
                        android:textAllCaps="true"
                        android:textColor="@color/text_primary"
                        android:textSize="@dimen/text_caption"
                        android:textStyle="bold" />

                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/sideMenuCategoryList"
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        android:layout_weight="1"
                        android:scrollbars="none" />
                </LinearLayout>
            </FrameLayout>
        </LinearLayout>
        <!-- Up Next / Auto-advance overlay -->
        <LinearLayout
            android:id="@+id/upNextOverlay"
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="24dp"
            android:background="@drawable/bg_player_bottom"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="16dp"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/up_next"
                android:textAllCaps="true"
                android:textColor="@color/primary"
                android:textSize="@dimen/text_caption"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/upNextTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/content_body_text"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/upNextCountdown"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="60"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/text_display"
                android:textStyle="bold" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:gravity="center_horizontal"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/upNextPlayNow"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:background="@drawable/bg_play_button"
                    android:clickable="true"
                    android:focusable="true"
                    android:gravity="center"
                    android:paddingHorizontal="16dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:text="@string/play_now"
                    android:textAllCaps="true"
                    android:textColor="@color/text_primary"
                    android:textSize="@dimen/text_caption"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/upNextCancel"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="12dp"
                    android:background="@drawable/bg_toolbar_button"
                    android:clickable="true"
                    android:focusable="true"
                    android:gravity="center"
                    android:paddingHorizontal="16dp"
                    android:stateListAnimator="@animator/focus_scale"
                    android:text="@string/cancel"
                    android:textAllCaps="true"
                    android:textColor="@color/text_primary"
                    android:textSize="@dimen/text_caption"
                    android:textStyle="bold" />
            </LinearLayout>
        </LinearLayout>
    </FrameLayout>
</FrameLayout>
